name: ProxyStore

channels:
    - defaults
    - conda-forge

dependencies:
    - python>=3.7.*
    - pip
    - redis
    - pip:
        - -e .
        - -r requirements-dev.txt
        - funcx
        - funcx_endpoint
